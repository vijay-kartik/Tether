<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the SDK's own subdirectory of the app cache is ever shared, never the whole of it. -->
    <cache-path name="tether-media" path="tether-media/" />
</paths>
